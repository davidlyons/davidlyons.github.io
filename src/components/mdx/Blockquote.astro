---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'blockquote'>;

const props = Astro.props;
---

<blockquote class="blockquote" {...props}>
  <slot />
</blockquote>
